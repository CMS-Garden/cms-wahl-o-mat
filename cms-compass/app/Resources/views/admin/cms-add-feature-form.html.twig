{% extends "base.html.twig" %}

{% block title %}Add feature to CMS {{ cms.name }} - CMS Garden CMS Compass {% endblock %}

{% block body %}

    <h1>
        Add feature to CMS {{ cms.name }}
    </h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ path('admin_show_cms_details', {'cmsId': cms.cmsId}) }}">Cancel</a>
    {{ form_end(form) }}

{% endblock %}